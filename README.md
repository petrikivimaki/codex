# CODEX

Open data directory and exploration tool for common infrequently updated datasets.

Run `npm run dev` and open [localhost:4242](http://localhost:4242/).

Defaults live in `config/app-config.json`. Personal overrides go in the ignored `config/app-config.local.json`; only changed settings are needed.
